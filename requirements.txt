# Core runtime
httpx>=0.27
psycopg[binary]>=3.1
pydantic>=2.6
python-dotenv>=1.0

# Scheduler (27-slot APScheduler + SQLite job store)
APScheduler>=3.10
SQLAlchemy>=2.0

# Ingestion adapters
telethon>=1.36
patchright>=0.5
duckduckgo-search>=6.0  # kept for environments that haven't upgraded yet
ddgs>=0.1               # renamed package — used preferentially
firecrawl-py==4.38.0    # V1FirecrawlApp / V1JsonConfig signature — pin exactly

# Local embeddings (semantic dedup — no API key needed)
# sentence-transformers downloads ~90 MB on first run and caches locally.
# If you can't install torch, remove this line; the pipeline falls back to
# exact SHA-256 URL dedup only (still works fine).
sentence-transformers>=3.0
torch>=2.2

# Testing
pytest>=8.0